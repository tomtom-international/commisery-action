# SPDX-FileCopyrightText: Copyright (c) 2018 GitHub, Inc. and contributors
# SPDX-License-Identifier: MIT

---

name: Check dist/

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'
  workflow_dispatch:

jobs:
  check-dist:
    name: Check dist/
    uses: actions/reusable-workflows/.github/workflows/check-dist.yml@main
